## Install

```bash
$ npm install
```

## Run the tests

```bash
# headless
$ npx cypress run

# headless with tags
$ npx cypress run --env tags=@tag

# with visual interface
$ npx cypress open

# with visual interface and tags
$ npx cypress open --env tags=@tag
```